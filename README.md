# invoice-generator-api
It is an api for the automatique generation of invoice

Microservice de facturation (FastAPI + WeasyPrint).

## Démarrage rapide (dev)

1. Créer un virtualenv
```bash
python -m venv .venv
source .venv/bin/activate   # sur PowerShell: .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
